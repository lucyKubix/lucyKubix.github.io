{% comment %}  Main navigation  {% endcomment %}
<nav class="flex wrap">
  {% for item in site.data.navigation.toc %}
    <a href="{{ item.url }}" {% if item.url == page.url or page.parent == item.url %}class="active"{% endif %}>{{ item.title }}</a>
  {% endfor %}
</nav>